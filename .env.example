# Database
# Primary connection URL (used for queries)
DATABASE_URL="postgresql://username:password@localhost:5432/appmarket?schema=public"
# Direct connection URL (used for migrations, bypasses connection pooling)
DIRECT_URL="postgresql://username:password@localhost:5432/appmarket?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"

# Encryption secret for data at rest (MUST be different from NEXTAUTH_SECRET)
# Generate with: openssl rand -hex 32
ENCRYPTION_SECRET=

# Optional - GitHub OAuth provider (not yet configured)
GITHUB_ID="your-github-oauth-client-id"
GITHUB_SECRET="your-github-oauth-client-secret"

# Optional - Google OAuth provider (not yet configured)
GOOGLE_ID="your-google-oauth-client-id"
GOOGLE_SECRET="your-google-oauth-client-secret"

# Twitter/X OAuth 2.0 (for account linking/reputation)
TWITTER_CLIENT_ID="your-twitter-oauth2-client-id"
TWITTER_CLIENT_SECRET="your-twitter-oauth2-client-secret"

# Stripe (for fiat payments)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Solana Network Configuration
# For production, use a mainnet RPC (Helius, QuickNode, etc.)
NEXT_PUBLIC_SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"
NEXT_PUBLIC_SOLANA_NETWORK="mainnet-beta"

# Platform wallet addresses
# These have defaults in code but can be overridden here
NEXT_PUBLIC_PROGRAM_ID="9udUgupraga6dj92zfLec8bAdXUZsU3FGNN3Lf8XGzog"
NEXT_PUBLIC_TREASURY_WALLET="3BU9NRDpXqw7h8wed1aTxERk4cg5hajsbH4nFfVgYkJ6"

# $APP Token (mainnet SPL token)
NEXT_PUBLIC_APP_TOKEN_MINT="Ansto3G3SzGt6bXo3pMddiM4YkW9Yt8y7Qvwy47dBAGS"

# USDC (mainnet)
NEXT_PUBLIC_USDC_MINT="EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"

# Platform settings
NEXT_PUBLIC_PLATFORM_FEE_BPS="500"  # 5%
NEXT_PUBLIC_DISPUTE_FEE_BPS="200"   # 2%
NEXT_PUBLIC_TOKEN_FEE_BPS="100"     # 1% of token supply

# Auto-buyback feature (future)
# Set to "true" to enable automatic buyback of platform token with revenue
ENABLE_AUTO_BUYBACK="false"
BUYBACK_PERCENTAGE="20"  # 20% of revenue used for buyback

# Optional - not currently used
UPLOADTHING_SECRET="sk_..."
UPLOADTHING_APP_ID="..."

# Vercel Blob (for profile pictures)
# Get this from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_XXXXXXXXXXXX"

# Site URL
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Admin/Security Configuration
# ADMIN_SECRET is required for admin API endpoints (reset-listings, etc.)
# Generate with: openssl rand -hex 32
ADMIN_SECRET="your-admin-secret-change-in-production"

# CRON_SECRET is used to authenticate scheduled tasks/cron jobs
# Generate with: openssl rand -hex 32
CRON_SECRET="your-cron-secret-change-in-production"

# Backend authority keypair for Solana contract operations (verify_uploads, etc.)
# This is the JSON-encoded secret key of the backend authority wallet
# Generate a keypair with: solana-keygen new --outfile backend-authority.json
# Then copy the array of numbers from that file here
BACKEND_AUTHORITY_SECRET_KEY="[1,2,3,...,64]"

# Privy Auth (optional - for social login)
# Get these from: https://console.privy.io
NEXT_PUBLIC_PRIVY_APP_ID="your-privy-app-id"
PRIVY_APP_SECRET="your-privy-app-secret"

# PATO (Post-Acquisition Token Offering) - Meteora DBC
# DBC config key created at https://launch.meteora.ag
PATO_DBC_CONFIG_KEY="your-dbc-config-key"
# Wallet address that claims platform trading fees
PATO_FEE_CLAIMER_WALLET="your-fee-claimer-wallet"
